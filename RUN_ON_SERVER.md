# DDTAD edition5 —— 第二轮探针结论 + 最终超参 + 全量跑分（多卡自适应 + 超订）

> 本文档随 `edition5/` 一起上传。**先看第一节（结论），再执行第三节（全量跑分）。**
> 本版把 GPU 数量改成**自动探测**，并按**每卡 2 进程**超订。

---

## 〇、为什么 GPU 利用率只有 ~28%（实测 nvidia-smi）

单卡跑一个进程时，`nvidia-smi` 显示 **~28% 利用率、~90W/450W**。这不是配置错误，是**任务本身喂不饱 GPU**：

| 指标 | 数值 |
|---|---|
| 模型参数量 | ~1.2 M（`base=32, ch_mult=(1,2,4)`） |
| 单次迭代（batch=128, fwd+bwd） | ~10.6 GFLOP |
| 4090 非 TF32 FP32 峰值 | ~40 TFLOPS |
| **理论下限** | **0.265 ms / 迭代** |
| **实测** | **~10 ms / 迭代** |
| **实际算力利用率** | **≈ 2.7%** |

即典型的 **kernel-launch / Python 开销受限（launch-bound）**，不是算力受限。
（每次迭代有上百次小 kernel 启动，加上 optimizer + EMA 的逐张量 Python 循环。）

**正确解法是「每卡多开进程」填满 GPU，而不是把模型改大** —— 模型改大会改变算法与结果，
多开进程则完全不改变训练动态。每进程显存 <1GB，卡有 24GB，超订没有 OOM 风险。

所以 edition5 把默认值改成：

```bash
WORKERS=${WORKERS:-$((NGPU * 2))}    # 每卡 2 进程；想保守就 WORKERS=$NGPU
```

另外新增了**实时进度 + ETA**，不用再 `tail` 日志：

```
[进度]   180s  已完成 6/81   s0:1/9  s1:1/8  s2:1/8  s3:1/8  s4:1/8  s5:0/8 ...   ETA≈31.5min
```

> 如果 `[进度]` 的 ETA 明显偏大或长时间不动，把 `WORKERS` 再往上调（`WORKERS=15` = 每卡 3 进程），
> 或者用 `--iters` 减半试跑一小批看速度。**注意机器是共享的**（GPU0 上有别人的任务），
> 超订过头会互相抢 CPU，一般 2~3 进程/卡是甜点。

---

## 一、第二轮探针（edition2）的结论

### ✅ 1. 恒零列根因**确认修好** —— MSL 的 loss 掉了 20 倍

| 通道 | 数据 | `zero`（旧） | **`drop`（新）** | 送入网络列数 |
|---|---|---|---|---|
| T-4 | MSL | 0.4920 | **0.0256** | 55 → 14 |
| C-1 | MSL | 0.4873 | **0.0393** | 55 → 18 |
| M-1 | MSL | 0.4991 | **0.0198** | 55 → 13 |
| P-1 | SMAP | 0.0829 | **0.0380** | 25 → 18 |
| E-1 | SMAP | 0.0902 | 0.0890 | 25 → 24 |
| D-1 | SMAP | 0.0852 | 0.0840 | 25 → 24 |
| D-12 | SMAP | 0.0824 | **0.0079** | 25 → **8** |

（数值为 eps-MSE @ t=999，越小越好；<0.1 算学会）

### ✅ 2. 意外收获：D-12 / D-13 被救回
D-12 训练段 25 列**全部恒定**，旧策略下归一化后训练输入是全 0 矩阵。
`drop` 策略给它在测试段有变化的 8 列正确尺度后：

| D-12 | 旧 `zero` | 新 `drop` |
|---|---|---|
| eps-MSE | 0.0824（学的是全 0） | **0.0079** |
| 点级 plain F1 | ≈0 | **0.512** |
| 点级 PA F1 | ≈0 | **0.514** |
| **oracle 上界** | ≈0 | **0.880** |

### ✅ 3. `t_start = 50` 最好（而不是之前猜的 200~400）

`gap`（异常均分/正常均分，越大越好）随 `t_start` 变化：

| 通道 | 50 | 100 | 200 | 300 | 400 |
|---|---|---|---|---|---|
| E-1 | **2.29** | 2.29 | 1.95 | 1.60 | 1.37 |
| T-4 | **2.09** | 1.90 | 1.44 | 1.19 | 1.07 |
| C-1 | **1.48** | 1.46 | 1.30 | 1.20 | 1.15 |
| P-1 | **1.08** | 1.08 | 1.05 | 1.04 | 1.03 |
| D-12 | **4.99** | 4.33 | 4.00 | 4.07 | 4.28 |
| D-1 | 7.48 | 9.83 | **10.52** | 9.38 | 7.95 |

**6/7 个通道在 t_start=50 最优，而且 50 步反向链也最便宜**（比 400 步快 8 倍）。
这印证了前面的判断：真正起作用的是**低噪声下的"去噪自编码器"机制**，不是论文描述的
"把异常抹掉再生成"。t_start=50 时 `alpha_bar=0.992`，信号保留 99.6%。

### 📊 4. 各通道最终表现（`drop` + `t_start=50` + `paper` + η=2）

| 通道 | 数据 | 类 | gap | plain F1 | PA F1 | η=4 PA | oracle | best PA (全网格) |
|---|---|---|---|---|---|---|---|---|
| **D-1** | SMAP | point | 7.48 | **0.929** | **0.986** | 0.995 | 0.932 | 0.996 |
| **P-1** | SMAP | contextual | 1.08 | 0.040 | **0.916** | 0.798 | 0.135 | 0.916 |
| **E-1** | SMAP | contextual | 2.29 | 0.105 | 0.694 | 0.886 | 0.364 | 0.886 |
| **D-12** | SMAP | point | 4.99 | 0.512 | 0.514 | 0.514 | 0.880 | 0.607 |
| **M-1** | MSL | contextual | 0.87 | 0.021 | 0.996 | 0.998 | 0.428 | 0.998 |
| **C-1** | MSL | point+ctx | 1.48 | 0.109 | 0.876 | 0.907 | 0.265 | 0.923 |
| **T-4** | MSL | point | 2.09 | 0.066 | 0.738 | 0.896 | 0.087 | 0.908 |

**7 通道平均 PA F1 = 0.817**（论文 SMAP 0.918 / MSL 0.958）。
注意这 7 个是**故意挑的难通道**（含 D-12 全恒定、T-4 单点弱异常、M-1 50% 异常）——
全量 81 通道的平均值应该更高。

### ⚠️ 5. 仍未完全解决的问题（诚实说明）

- **点级 plain F1 普遍很低（0.02~0.11）**，oracle 上界也低（0.087~0.43）。
  说明重构误差的**逐点判别力本身偏弱**，PA 协议做了主要贡献。
- **T-4 是单变量弱异常**（异常区 |z| 只有 1.84 倍），逐变量 top1 误差比 9.66×，
  但 24 列取均值后 gap 只剩 2.09，逐点 oracle 仅 0.087 —— 这类通道确实难。
- **M-1 是退化通道**：异常占 50.11%，"全判异常"就有 F1=0.668，而 oracle 只有 0.428
  → 该通道得分与真值**反相关**。它会把宏平均拉低。
- **E-1 的 `val_sd` 被离群撑大**（0.00283 vs 测试正常段 0.00196）导致 η=2 时召回偏低，
  η 提到 4 时 PA 反而升到 0.886。稳健阈值 `val_robust` 在本轮实测中**没有更好**。

---

## 二、edition3~5 相对 edition2 的改动

| # | 文件 | 改动 |
|---|---|---|
| 1 | `ddtad_run.py` | **方案扫描**：重构算完后，一次性评估「8 种打分×阈值×平滑」组合，全部写进 `results.json` / `report.txt` 末尾的汇总表。一次全量跑分就能拿到所有备选方案 |
| 2 | `ddtad_run.py` | 新增打分 `zratio_mean` / `zratio_max`：除以测试段自身中位数（相对提升倍数）再 clip，兼顾"逐变量去偏"和"多变量聚合"，且不会像 z-score 那样数值爆炸 |
| 3 | `ddtad_run.py` | 新增 `--smooth W` 居中滑动平均（SMAP/MSL benchmark 标准做法，对长 contextual 异常有效） |
| 4 | `ddtad_run.py` | `zval_*`/`zself_*` 加数值保护（D-12 的验证段恒定会让 z 爆到 4e5） |
| 5 | `run_all.sh` | 默认超参改为探针定下的最优：`--col_policy drop --t_start 50 --score paper --thresh val --eta 2 --n_sample 3` |
| **6** | **`run_all.sh` / `run_probe.sh`** | **GPU 数量改为自动探测**（`torch.cuda.device_count()`，尊重 `CUDA_VISIBLE_DEVICES`）。4/5/8 卡都不用改代码 |
| **7** | **`ddtad_parallel.py`** | 父进程**不再初始化 CUDA 上下文**（旧版会在 GPU0 上多占一份显存，而 GPU0 还要跑 shard0）；新增 `gpu_banner()` 枚举每张卡 |
| **8** | **`run_probe.sh`** | 多卡自检用 **6 个通道**，保证 ≥5 张卡时**每张卡都能分到活干**，能真正验证 5 卡并行 |

---

## 三、上传与执行

```bash
scp -r edition5 liuqr@pserver4090:/mnt/sdb/home/liuqr/Project/20269/ddtad/

cd /mnt/sdb/home/liuqr/Project/20269/ddtad/edition5

# 什么都不用指定 —— 自动探测到 5 张卡
bash run_all.sh

# 想显式指定 / 每卡开 2 进程（小模型常受 kernel launch 限制，超订有时更快）
NGPU=5 bash run_all.sh
WORKERS=10 bash run_all.sh
```

`run_all.sh` 会先打印**负载均衡分片方案**（每个 shard 绑哪张卡、多少通道、多少点）让你确认，
然后 81 个通道并行跑。

预计耗时（5×4090）：**约 20~25 分钟**。单卡参考：~1.5 小时。

只想先验证一下可以加 `--limit`：
```bash
python ddtad_run.py --data_dir /mnt/sdb/home/liuqr/Dataset --channel all --limit 8 --out_dir out/try
```

---

## 三-b、多卡适配核验清单（改完已逐项核对）

| 环节 | 实现 | 是否随卡数自动适配 |
|---|---|---|
| 卡数探测 | `torch.cuda.device_count()`（`run_all.sh` L22 / `ddtad_parallel.py` L92） | ✅ 4/5/8 卡通吃，尊重 `CUDA_VISIBLE_DEVICES` |
| 设备绑定 | `torch.cuda.set_device(idx)`（`pick_device`） | ✅ `--gpu N` 单卡调试用 |
| TF32 / cudnn autotune | `cudnn.benchmark` + `allow_tf32` | ✅ 每次进 `pick_device` 都开 |
| AMP | `_amp_api()` 跨 torch 版本兼容，不可用自动降级 | ✅ `--amp` |
| 任务分片 | `shard_by_cost()` 按 **test_len 贪心** 均衡（L56-68） | ✅ 分片数 = `--workers` |
| 卡↔进程映射 | `gpu = i % n_gpu`；`env["CUDA_VISIBLE_DEVICES"]=gpu`（L132/L139） | ✅ `workers>ngpu` 自动超订 |
| 子进程隔离 | `subprocess.Popen(env=..., cwd=...)`，各自 `shardN.log` | ✅ |
| 结果合并 | `write_summary()`（主程序） / `probe_report_all.txt`（探针） | ✅ |
| 父进程显存 | 刻意**不**初始化 CUDA 上下文 | ✅ 不浪费 GPU0 显存 |
| 逐卡健康检查 | `[0/7]` 阶段对**每一张卡**各做一次 TF32 矩阵乘 | ✅ 5 张卡逐个验 |
| OOM 兜底 | 失败时 `torch.cuda.empty_cache()`；`[7/7]` 扫描分片日志 | ✅ |

> 自检阶段（`run_probe.sh` 的 [2/7]、[3/7]）会用 **6 个通道**跑一遍完整多卡链路，
> 校验报告里通道数正确、`summary.csv` 行数正确，失败会 `exit 1` 直接停下。

---

## 四、跑完要回传什么

1. **`out/all/report.txt`** ← 最重要，末尾有「方案扫描」汇总表
2. **`out/all/summary.csv`** ← 81 通道逐通道指标
3. 若某个 shard 报错：`out/all/shard*.log` 与 `out/all.launch.log`

我会根据「方案扫描」表决定是否把 `--score` / `--smooth` 换成更优组合，然后给你对齐论文表格的最终结果。

---

## 五、完整问题清单（累计）

| 编号 | 问题 | 后果 | 状态 |
|---|---|---|---|
| P1 | `beta=linspace(1e-4,0.02,T=100)` → `alpha_bar[T-1]=0.3636` | 加噪后残留 60% 信号；反向链放大 1.66 倍，重建 MSE 2.257（比不做还差 3 倍） | ✅ 已修 |
| P2 | 原 1-D U-Net = ReLU + 无归一化层 | ε-MSE 卡在 0.62~0.99，网络没学到 | ✅ 已修（0.03） |
| P3 | 训练不足 / 无 LR 调度 / 无 EMA | 欠训练 | ✅ 已修 |
| P4 | 验证集从重叠窗口随机抽 20% | 阈值偏低 → 误报爆炸 | ✅ 已修 |
| P5 | 测试窗口 128 不重叠，粒度太粗 | 与论文点级指标不可比 | ✅ 已修 |
| P6 | 反向采样随机无降方差 | 得分方差大 | ✅ 已修（DDIM + n_sample） |
| P7 | test 用自身 min/max 归一化 | 分布外输入 | ✅ 已修 |
| P8 | 得分只对变量取均值 | 异常被稀释 | ✅ 已修（8 种打分） |
| P9 | 分布漂移时论文阈值"全判异常" | 阈值失效 | ✅ 已修（3 种阈值 + oracle） |
| P10 | GPU 迁移只做一半 | 4 张卡用 1 张 | ✅ 已修（TF32/AMP/通道级并行） |
| **P11** | **恒零列污染损失** | **MSL 27 通道全废** | ✅ **已修（`--col_policy drop`）** |
| **P12** | **FiLM `sigmoid(scale)` 只能衰减** | 高信噪比区域是硬约束 | ✅ 已修 |
| P13 | D-12/D-13 归一化后训练输入全 0 | 模型学不到东西 | ✅ 已修（`drop` 顺带解决） |
| P14 | `--channel all` 未实现、无随机种子、KMP 冲突 | — | ✅ 已修 |
| **P15** | **逐点判别力偏弱（plain F1 低）** | 只能靠 PA 拿分 | ⚠️ 待评估（方案扫描里试平滑与 zratio） |
| **P16** | **M-1 类退化通道（异常占 50%）** | 得分反相关，拉低宏平均 | ⚠️ 已知，报告中单独标注 |
