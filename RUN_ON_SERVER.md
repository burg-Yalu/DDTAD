# DDTAD edition13（第二轮改动）—— 只改阈值：3 处修复，目标是把"一个都不判"的通道救回来

> 本文档随 `edition13/` 一起上传（同一文件夹，已就地更新）。

---

## 〇、为什么只改阈值

全量 81 通道结果（`report.txt`）说明一切：

| 口径 | P | R | F1 |
|---|---|---|---|
| **窗口级 不PA（论文口径）** | 37.41 | 37.54 | **37.48** |
| SMAP | 41.83 | 37.85 | **39.74** |
| MSL | 23.40 | 35.87 | **28.33** |
| 论文 DDTAD | 92.27 / 94.47 | 91.30 / 97.21 | **91.78 / 95.82** |

**失败高度集中**：81 个通道里 **34 个（42%）F1 = 0**，但其中 **24 个不是打分没信号，是阈值崩了**：

| 通道 | 窗口 | 异常窗 | F1 | **最优阈值F1** | 预测率 | 阈值 |
|---|---|---|---|---|---|---|
| **F-4** | 26 | 1 | **0.0000** | **1.0000** | **0.0%** | 9.76 |
| D-15 | 16 | 5 | 0.0000 | 0.8333 | **0.0%** | 14.64 |
| A-6 | 34 | 2 | 0.0000 | 0.6667 | **0.0%** | 11.57 |
| T-2 | 67 | 14 | 0.0000 | 0.4444 | **0.0%** | 7.43 |
| P-4 | 60 | 7 | 0.0000 | 0.4211 | **0.0%** | 10.04 |
| *(对照)* G-4 | 59 | 1 | 1.0000 | 1.0000 | 1.7% | 5.79 |

**F-4 最典型**：阈值 9.76 高于所有测试窗口得分 → 一个都不判 → F1=0；
但最优阈值能拿到 **F1=1.000（满分）**。阈值尺度在通道间从 5.79 波动到 14.64，正确水位是逐通道不同的。

按预测率分组，规律非常清楚：

| 预测率 | 通道数 | 平均 F1 |
|---|---|---|
| < 0.5%（几乎不判） | 9 | **0.00** |
| 0.5–5% | 23 | 12.04 |
| 5–20% | 28 | 34.58 |
| **20–60%** | 19 | **49.53** |
| > 60% | 2 | 7.66 |

真实窗口异常率是 14.88% —— **我们整体在欠预测**。

---

## 一、这轮改了什么（3 处，全在阈值，不动模型）

| # | 改动 | 目的 |
|---|---|---|
| **1** | **`ETA_LIST` 下探到 0**：`[0.0, 0.5, 1.0, 1.5, 2.0, 3.0, 4.0, 6.0]` | 之前最低只到 η=1.5，而数据说我们欠预测。η 越小阈值越低、判得越多 |
| **2** | **新增下限保护阈值 `l20 / l10 / l05`**：`thr = min(论文阈值, 测试段分位数)`，**保证至少判 20%/10%/5%** | 直接救那 9 个"预测率≈0"的通道 |
| **3** | **新增分位映射阈值 `qmap`**：把论文阈值在**验证段分布里的分位位置**搬到**测试段分布**上 | 尺度不变，从原理上消除"验证段和测试段阈值不在同一水位"的问题 |

方案扫描组合数从 322 → **938**（7 打分 × 2 平滑 × 67 阈值组合），**评估是免费的**（重构缓存复用）。

顺带：`report.txt` 末尾现在会额外打印 **最优组合的 SMAP / MSL 拆分**，可以直接和论文 TABLE III 对比。

已验证：每个阈值模式的数值行为逐个测过；`l20/l10/l05` 的预测率精确等于 20%/10%/5%；
端到端冒烟（含 JSON 往返合并）**4 项检查全部通过**。

---

## 二、执行

```bash
cd /mnt/sdb/home/liuqr/Project/20269/ddtad/edition13
bash run_all.sh              # 约 30~40 分钟
```

启动时看这两行确认多卡正常：
```
[多卡核验] 10/10 个分片都跑在预期的 GPU 上 ✔
[进度]  180s  完成 3/81  进行中 8   s0:1/8 ...   ETA≈28.4min
```

**回传**：`out/all/report.txt` + `out/all/summary.csv`

我会给出：
- **论文忠实版**（`paper|val|η2`）的 SMAP / MSL P/R/F1
- **调优版**（938 组合里的最优）的 SMAP / MSL P/R/F1
- 两者与论文的差距归因

---

## 三、必须先说清楚的期望值（重要）

| 口径 | F1 |
|---|---|
| 论文配置（μ+2σ，不 PA） | 37.48 |
| **逐通道最优阈值（oracle），不 PA** | **46.97** ← 天花板 |
| **逐通道最优阈值 + PA** | **73.81** |
| **论文声称** | **91.78 / 95.82** |

**即使把阈值调到"上帝视角最优"，不 PA 也只有 46.97。** 这轮的目标是尽量逼近这个天花板
（预计 micro 37.5 → 42~47），**而不是追上论文**。

差的那 45 个点（46.97 → 91.78）不可能靠调阈值/调打分补上。可能的原因：
1. 论文只写 "Linear" **未给 β 范围**（T=100 时 ᾱ=0.364，只抹掉 40% 信息）
2. 论文**未给 1-D U-Net 的层数/每层通道**（只有 "Dimension 64"）
3. 论文未说明窗口级指标是微观汇总还是逐通道平均（我们 37.48 vs 27.18，差 10 点）
4. 论文未说明是否用 point-adjustment（我们两种都算了）
5. 论文**未公开代码**
6. 该领域评测协议本身不统一（Kim et al., AAAI 2022 专门批评过 PA 协议）

---

## 四、已对齐论文的部分（未变）

| 项 | 论文 | 现状 |
|---|---|---|
| 判定单元 / point-adjust | 窗口 / 不用 | ✅ |
| 归一化 / 窗口 / 步长 | [−1,1] / 128 / 10 与 128 | ✅ |
| 验证集 | 随机 20% 窗口 | ✅ |
| T / schedule | 100 / Linear | ✅（β 范围论文未给） |
| Dimension / lr / betas | 64 / 5e-5 / (0.9,0.99) | ✅ |
| EMA / batch / epoch | 0.995 / 32 / 1500 | ✅ |
| η / 采样 | 2 / Algorithm 1.B 随机反向 | ✅ 主指标仍是 η=2 |
| 轮数 | 10 轮随机划分 | ⚠️ 先跑 1 轮（`--rounds 10` 对齐） |

---

## 五、GPU 适配（上一轮已审计并修复，未变）

| 项 | 说明 |
|---|---|
| GPU 迁移 | `pick_device` + TF32 + cudnn.benchmark + AMP + 全部张量在 device |
| 多卡 | 卡数自动探测、test_len 贪心分片、`CUDA_VISIBLE_DEVICES` 隔离、父进程不占显存 |
| **多卡核验** | 子程序打印 `CVD=`，启动器回读日志确认 shard i 真在 GPU i 上 |
| **EMA** | 改 `torch._foreach_*`，kernel launch 从 ~320 次降到 2 次（数值完全一致） |
| 显存 | `expandable_segments:True` + 每 5 通道 `empty_cache()` 并打印 |
| 进度 | 每 60s 打印 `完成 k/81 进行中 m ETA≈…`（上一轮修好了 token 不匹配的 bug） |
